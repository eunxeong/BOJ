#include <iostream>
#include <queue>
#include <algorithm>
#include <vector>

using namespace std;
// 1826 연료 채우기

struct OIL{
	int p;
	int f;
};

bool cmp1(OIL a, OIL b){
	return a.p < b.p;
}

bool cmp2(OIL a, OIL b){
	return a.f > b.f;
}

int main(){
	
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	
	int N;	cin >> N;
	vector<pair<int, int> > V;
	
	for(int i = 0; i < N; i++){
		
		
	}
	
	
	
}
